# Thư mục migration TypeORM (chỉ tiến về trước — coding-rules 2.6).
# Tạo migration đầu tiên khi có entity (T016+):
#   pnpm --filter @lms/api migration:generate src/database/migrations/<TenMigration>
